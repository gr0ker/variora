# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is in its initial state. It currently contains only a `LICENSE`
file (MIT, 2026 gr0ker) — there is no source code, build system, dependencies,
or tests yet.

There are no build, lint, test, or run commands to document at this point.

## When adding the first code

Once the project takes shape, expand this file with the details that are not
obvious from reading individual files:

- **Commands** — how to build, run, lint, and test (including running a single
  test) once a package manifest / build tooling exists.
- **Architecture** — the big-picture structure and how the major pieces fit
  together, the kind of understanding that requires reading several files.

Re-running `/init` after real code lands will regenerate this guidance from the
actual codebase.
